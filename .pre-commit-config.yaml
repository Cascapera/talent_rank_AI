repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Tem que bater com o ruff de requirements-dev.txt e do job de lint no CI.
    rev: v0.15.17
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
