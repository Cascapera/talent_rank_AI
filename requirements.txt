# Dependências de produção — VERSÕES TRAVADAS (R-02).
#
# Estas são exatamente as versões que rodam em produção hoje, colhidas por
# `pip freeze` no servidor em 2026-08-17 (Python 3.10.12, Ubuntu).
#
# POR QUE TUDO ESTÁ FIXO, inclusive as transitivas:
# o `deploy.yml` roda `pip install -r requirements.txt` a cada push em `main`.
# Sem `==`, um release novo de qualquer pacote entra em produção sozinho, num
# deploy que talvez só mudasse um texto. Fixar só as diretas deixaria ~30
# transitivas flutuando — um `pydantic` novo quebraria o `google-genai` do
# mesmo jeito, só que um nível abaixo.
#
# COMO ATUALIZAR UMA VERSÃO: é PR próprio, nunca junto de outra mudança.
# Altere o pin, rode `pip install -r requirements.txt` num venv 3.10 limpo,
# rode a suíte e refaça o freeze para atualizar as transitivas junto.
#
# NÃO ADICIONE `pypdf` AQUI. Ele ainda está instalado no servidor porque
# `pip install` nunca desinstala, mas ficou sem uso nenhum no R-03 (era só do
# parser de currículo que foi removido).

# --- Diretas ---
Django==5.2.10
psycopg[binary]==3.3.2
python-dotenv==1.2.1
google-genai==1.60.0
gunicorn==24.1.1
whitenoise==6.11.0
prometheus_client==0.24.1

# --- Transitivas (resolvidas pelas diretas acima; não importar direto no código) ---
annotated-types==0.7.0
anyio==4.12.1
asgiref==3.11.0
certifi==2026.1.4
cffi==2.0.0
charset-normalizer==3.4.4
cryptography==46.0.3
distro==1.9.0
exceptiongroup==1.3.1
google-auth==2.48.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
packaging==26.0
psycopg-binary==3.3.2
pyasn1==0.6.2
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.12.5
pydantic_core==2.41.5
requests==2.32.5
rsa==4.9.1
sniffio==1.3.1
sqlparse==0.5.5
tenacity==9.1.2
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.6.3
websockets==15.0.1
